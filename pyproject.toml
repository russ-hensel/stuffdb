[project]
name = "StuffDB"
version = "0.1.0"
description = "A database to keep track of all kinds of stuff"
readme = "README.md"
requires-python = ">=3.13"

dependencies = [
    "unidecode>=1.3.0",
    "wat-inspector"
]

